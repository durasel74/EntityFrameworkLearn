<UserControl x:Class="EntityFrameworkLearn.View.Controls.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EntityFrameworkLearn.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100" 
             d:HorizontalContentAlignment="Center"
             d:VerticalContentAlignment="Center">

    <ToggleButton Width="40" Height="40"
                  FontWeight="ExtraBold" IsChecked="{Binding MenuVisible}">
        <Viewbox>
            <TextBlock Text="☰"/>
        </Viewbox>
    </ToggleButton>
</UserControl>
